# Example 018 · Paper and margins

Setting the page size and, more importantly, the margin as a single argument:
a per-side dictionary `(x:, y:)` here, with notes on `paper: "a4"` and the
`flipped: true` landscape switch. Used in
[Chapter 5, *Pages and layout*](../../book/05-pages-and-layout.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
