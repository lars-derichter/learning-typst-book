# Example 43 · Aligned equations

A multi-line derivation using `\` for line breaks and `&` for the
alignment column, so every step lines up on its equals sign. Used in
[Chapter 8, *Math and equations*](../../book/08-math-and-equations.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
