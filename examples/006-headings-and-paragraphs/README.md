# Example 006 · Headings and paragraphs

Heading levels with `=`/`==`/`===`, how blank lines start paragraphs, how runs
of whitespace collapse, and the trailing-backslash hard line break. Used in
[Chapter 3, *Markup: the content layer*](../../book/03-markup-content-layer.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
