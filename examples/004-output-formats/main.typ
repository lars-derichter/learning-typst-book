// A short document used to demonstrate exporting to PDF, PNG, and SVG.
// See the README for the three compile commands.
#set page(width: 12cm, height: auto, margin: 1.5cm)

= One source, three formats

The same `.typ` file compiles to a PDF, a raster PNG, or a scalable SVG. You
pick the format with the output extension (or an explicit `--format`).

#lorem(35)
