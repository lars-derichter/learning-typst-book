# Example 041 · Sums and integrals

Big operators that carry limits — `sum_(i=1)^n`, `integral_a^b`,
`product`, and `lim_(x -> 0)` — showing how bounds stack in a block but
sit to the side inline. Used in
[Chapter 8, *Math and equations*](../../book/08-math-and-equations.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
