# Example 10 · Symbols and escapes

En/em dashes (`--`, `---`), the automatic ellipsis (`...`), the non-breaking
space (`~`), backslash escapes for literal specials, Unicode escapes
(`\u{…}`), and line/block comments. Used in
[Chapter 3, *Markup: the content layer*](../../book/03-markup-content-layer.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
