# Example 081 · break and continue

Filtering inside a loop: `continue` skips failing scores so only passes print,
and `break` stops the loop at the first perfect score. Both live inside an
`if` that inspects the current item. Used in
[Chapter 15, *Control flow*](../../book/15-control-flow.md).

```sh
typst compile main.typ out.pdf
```
