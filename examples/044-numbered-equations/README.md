# Example 044 · Numbered equations

`#set math.equation(numbering: "(1)")` to number block equations, a
`<eq:...>` label, and an `@eq:...` cross-reference that resolves to a
live link. Used in
[Chapter 8, *Math and equations*](../../book/08-math-and-equations.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
