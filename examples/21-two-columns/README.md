# Example 21 · Two columns

Whole-page columns with `#set page(columns: 2)`, filled with `#lorem` so the
gutter between the two columns is visible and the left-then-right flow is
clear. Used in
[Chapter 5, *Pages and layout*](../../book/05-pages-and-layout.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
