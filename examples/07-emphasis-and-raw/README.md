# Example 07 · Emphasis and raw text

`_emphasis_` and `*strong*` and how they nest, inline `` `code` `` with
backticks, and a fenced block with a language tag for syntax highlighting —
showing that raw text is verbatim. Used in
[Chapter 3, *Markup: the content layer*](../../book/03-markup-content-layer.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
