# Example 12 · Fonts and families

One sentence set in three bundled typefaces (Libertinus Serif, New Computer
Modern, DejaVu Sans Mono), each labeled, plus a `font: ("A", "B")` fallback
list. Used in
[Chapter 4, *Text and fonts*](../../book/04-text-and-fonts.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
