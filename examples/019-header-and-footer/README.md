# Example 019 · Header and footer

A running header (title on the left, chapter on the right) and a centered
footer holding the page number via `counter(page).display()`, using `#h(1fr)`
to align the pieces. Used in
[Chapter 5, *Pages and layout*](../../book/05-pages-and-layout.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
