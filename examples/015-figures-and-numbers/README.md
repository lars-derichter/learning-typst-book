# Example 015 · Figures and numbers

OpenType number styling with `number-type` (lining vs old-style) and
`number-width` (tabular vs proportional), including a small table where tabular
digits line up in a column. Used in
[Chapter 4, *Text and fonts*](../../book/04-text-and-fonts.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
