# Example 038 · Inline versus block

The whitespace rule that switches math between inline (`$x$`) and a
centered display block (`$ x $`), shown with the same equation both ways.
Used in [Chapter 8, *Math and equations*](../../book/08-math-and-equations.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
