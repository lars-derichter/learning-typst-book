# Example 071 · Interpolation

A small receipt whose subtotal, VAT, and total are computed once with `#let`
and spliced into a table with `#`. Change a price and every derived figure
updates. Used in
[Chapter 13, *From markup to code*](../../book/13-from-markup-to-code.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
