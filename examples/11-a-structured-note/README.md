# Example 11 · A structured note

A small but complete meeting note that combines everything in the chapter:
headings, emphasis, term and numbered lists, a labeled link, a raw shell
block, and an em dash. The kind of document you could actually write. Used in
[Chapter 3, *Markup: the content layer*](../../book/03-markup-content-layer.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
