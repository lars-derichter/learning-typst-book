# Example 014 · Color and decorations

Text `fill` via `rgb`, named colors, `luma`, and `cmyk`, plus the decoration
functions `#underline`, `#overline`, `#strike`, `#highlight`, and `#smallcaps`,
including restyled variants. Used in
[Chapter 4, *Text and fonts*](../../book/04-text-and-fonts.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
