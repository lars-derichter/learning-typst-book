# Example 23 · Spacing and breaks

Manual `#v(length)` vertical space, `#h(1fr)` stretchy horizontal space, and
the difference between a hard `#pagebreak()` and a `#pagebreak(weak: true)`
that collapses when it would leave a blank page. Used in
[Chapter 5, *Pages and layout*](../../book/05-pages-and-layout.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
