# Example 069 · Operators

Arithmetic (with precedence), comparison operators returning booleans, logical
`and`/`or`/`not`, and `+` as a joiner for strings, arrays, and content. Notes
that `/` always yields a float. Used in
[Chapter 13, *From markup to code*](../../book/13-from-markup-to-code.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
