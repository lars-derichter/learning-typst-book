# Example 39 · Symbols and scripts

Greek letters, relations, and arrows written by name (`alpha`, `<=`,
`->`, `oo`), plus `^`/`_` scripts and parenthesised grouping like
`x^(n+1)` and `a_(i j)`. Used in
[Chapter 8, *Math and equations*](../../book/08-math-and-equations.md).

```sh
typst compile main.typ out.pdf
```

Rendered preview: [`out.png`](out.png). Full output: [`out.pdf`](out.pdf).
